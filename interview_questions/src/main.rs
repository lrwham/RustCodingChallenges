mod first_occurence_k_times;
mod sub_array_sum;
mod helpers;

fn main() {
    println!("Hello, world!");
    first_occurence_k_times::first_occurence_k_times::solution();
    sub_array_sum::sub_array_sum::solution();
}
